global:
  resolve_timeout: 5m

route:
  receiver: 'default-receiver'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  group_by: ['alertname', 'job']

receivers:
  - name: 'default-receiver'
    # 可以添加邮件、Slack 等通知方式
    # webhook_configs:
    # - url: 'http://your-alert-webhook'
